{% extends 'base.html.twig' %}

{% block body %}
<section>
	<div class="container-fluid">
		<div class="row">
        <div class="col-10">
<table class="table table-striped table-bordered">
    <thead>
        <th>id</th>
         <th>title</th>
         <th>page</th>
        <th>total</th>
        <th>updatedat</th>
    </thead>
    <tbody>
   {% for bk in books %}
    <tr>
        <td>{{bk.id}}</td>
        <td>{{bk.title}}</td>
        <td>{{bk.current}}</td>
        <td>{{bk.total}}</td>

       
    </tr>
   {% endfor %}
    </tbody>
</table>
</div>
</section>
{% endblock %}
